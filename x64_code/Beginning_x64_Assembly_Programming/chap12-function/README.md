use dot in front of the variable to declare local variable (just a promise)

function can have their own .data and .bss section

function can not nest

main is just a function that calls other functions.
